# 测试环境：Python 3.12, Vercel Free Tier
# 移除 starlette 显式版本，让 FastAPI 自动处理（避免 <0.42.0 冲突）

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# 数据库（Vercel Postgres 免费层兼容）
SQLAlchemy==2.0.36
psycopg2-binary==2.9.9
alembic==1.13.3

# 认证/工具（如果用）
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# 其他（CORS 已包含在 Starlette/FastAPI 中）
python-multipart==0.0.9  # 文件上传
typer[all]==0.12.5      # CLI

# gunicorn（Vercel 不强制用，但备用多进程）
gunicorn==23.0.0